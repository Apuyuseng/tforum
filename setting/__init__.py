from .setting import xsrf_cookies, debug, static_path, cookie_secret, login_url

settings = {
    'xsrf_cookies': xsrf_cookies,
    'debug': debug,
    'static_path': static_path,
    'cookie_secret': cookie_secret,
    'login_url': login_url,
}
